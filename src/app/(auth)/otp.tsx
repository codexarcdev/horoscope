import React from "react";
import { OTPScreen } from "@/screen/auth";
import { Container } from "@/components";

const OTPVerify: React.FC = () => {
  return (
    <Container>
      <OTPScreen />
    </Container>
  );
};

export default OTPVerify;
